apiVersion: v1
kind: Service
metadata:
  name: app-card-server
  namespace: app-card-server
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: app-card-server

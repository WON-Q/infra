apiVersion: v1
kind: Service
metadata:
  name: wonq-order-merchant-client
  namespace: wonq-order-merchant-client
spec:
  type: LoadBalancer
  ports:
    - port: 443
      targetPort: 3000
      name: https
  selector:
    app: wonq-order-merchant-client

apiVersion: apps/v1
kind: Deployment
metadata:
  name: card-server
  namespace: card-server
spec:
  replicas: 3
  selector:
    matchLabels:
      app: card-server
  template:
    metadata:
      labels:
        app: card-server
    spec:
      containers:
        - name: card-server
          image: asia-northeast3-docker.pkg.dev/northern-window-458205-h4/fisa/card-server:latest
          ports:
            - containerPort: 8080
